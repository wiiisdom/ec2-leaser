{
  "name": "ec2-leaser",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "sst dev",
    "test": "sst test",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove",
    "typecheck": "tsc --noEmit",
    "sonar": "node sonar.js"
  },
  "devDependencies": {
    "@tsconfig/node16": "^1.0.3",
    "sonarqube-scanner": "^2.9.1",
    "sst": "^2.1.17",
    "typescript": "^4.9.5"
  },
  "dependencies": {},
  "workspaces": [
    "packages/*"
  ]
}
