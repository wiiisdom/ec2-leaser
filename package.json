{
  "name": "ec2-leaser",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "sst dev",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@tsconfig/node20": "^20.1.4",
    "sst": "2.43.1",
    "typescript": "^5.0.0"
  },
  "dependencies": {},
  "workspaces": [
    "packages/*"
  ]
}
