{
  "name": "@ec2-leaser/frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.12.0",
    "@testing-library/react": "^14.0.0",
    "@testing-library/user-event": "^14.4.3",
    "aws-amplify": "^5.0.15",
    "cross-env": "7.x",
    "prettier": "^2.x",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-query": "^3.24.4",
    "react-scripts": "^5.0.1",
    "react-svg-spinner": "^1.0.4"
  },
  "scripts": {
    "start": "sst env react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom ",
    "test:ci": "react-scripts test --env=jsdom --coverage --reporters 'default' --reporters 'jest-junit' --testResultsProcessor 'jest-sonar-reporter'",
    "format": "prettier --write \"./src/**/*.{js,jsx}\"",
    "sonar": "node sonar.js"
  },
  "jest-junit": {
    "outputDirectory": "test-reports"
  },
  "jestSonar": {
    "reportPath": "test-reports",
    "reportFile": "test-report.xml",
    "indent": 4
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "autoprefixer": "^10.4.2",
    "jest-junit": "^15.0.0",
    "jest-sonar-reporter": "^2.0.0",
    "postcss": "^8.4.6",
    "react-test-renderer": "^18.2.0",
    "sonarqube-scanner": "^3.0.1",
    "sst": "^2.1.17",
    "tailwindcss": "^3.0.22"
  }
}
